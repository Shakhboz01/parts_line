<div class="container my-5">
  <div class="card shadow">
    <div class="card-header d-flex justify-content-between align-items-center">
      <h3 class="card-title font-weight-bold">Providers</h3>
      <%= link_to 'Добавить новый', new_provider_path, class: "btn btn-success min-width-btn" %>
    </div>

    <div class="card-body">
      <table class="table table-hover table-bordered table-striped">
        <thead>
          <tr>
            <th><%= sort_link q, :id, 'ID' %></th>
            <th><%= sort_link q, :name, 'имя' %></th>
            <th><%= sort_link q, :phone_number, 'номер телефона' %></th>
            <th><%= sort_link q, :comment, 'Коммент' %></th>
            <th><%= sort_link q, :created_at, 'created at' %></th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% providers.each do |provider| %>
            <tr>
              <td><%= provider.id %></td>
              <td><%= provider.name %></td>
              <td><%= provider.phone_number %></td>
              <td><%= provider.comment %></td>
              <td><%= strf_datetime(provider.created_at) %></td>
              <td><%= clink_to 'Редактировать', edit_buyer_path(provider), 'fa-edit' %></td>
              <td><%= clink_to 'show histories translate', product_entries_path(q: {provider_id_eq: provider.id}), 'fa-edit' %></td>
              <%# <><%= clink_to 'Посмотреть', provider_path(provider), 'fa-search' %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <%= paginate providers %>
  </div>
</div>
